scope = "rig"
provider = "codex"
option_defaults = { model = "gpt-5.5" }
wake_mode = "fresh"
work_dir = ".t3-dev/worktrees/gascity/{{.CityName}}/{{.Rig}}/jj/agents/sentinel"
nudge = "Run gc prime, inspect JJ workspace health, and repair stuck workflow state."
idle_timeout = "1h"
max_active_sessions = 1
