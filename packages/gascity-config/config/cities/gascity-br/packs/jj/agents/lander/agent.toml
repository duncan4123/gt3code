scope = "rig"
provider = "kimi-for-coding"
option_defaults = { model = "kimi-for-coding/k2p6", agent = "build" }
wake_mode = "fresh"
work_dir = ".gc/jj/agents/{{.Rig}}/lander"
nudge = "Run gc prime, inspect submitted JJ changes, and land ready work serially."
idle_timeout = "2h"
max_active_sessions = 1
