scope = "rig"
provider = "codex"
option_defaults = { model = "gpt-5.5" }
wake_mode = "fresh"
work_dir = ".gc/jj/agents/{{.Rig}}/planner"
nudge = "Check mail, then create or refine stack-shaped work beads."
idle_timeout = "1h"
max_active_sessions = 1
