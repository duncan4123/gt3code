scope = "rig"
provider = "kimi-for-coding"
wake_mode = "fresh"
work_dir = "{{.RigRoot}}/.t3-dev/worktrees/gascity/{{.CityName}}/{{.Rig}}/jj/agents/lander"
nudge = "Run gc prime, inspect sentinel-approved JJ changes, optionally run landing gates, and land serially."
idle_timeout = "2h"
max_active_sessions = 1
