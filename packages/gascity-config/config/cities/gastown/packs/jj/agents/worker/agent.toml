scope = "rig"
provider = "codex"
option_defaults = { model = "gpt-5.5" }
wake_mode = "fresh"
is_pool = true
work_dir = "{{.WorktreesRoot}}/gascity/{{.CityName}}/{{.Rig}}/jj/agents/workers/{{.AgentBase}}"
nudge = "Run gc hook; claim one routed work bead, create a JJ workspace, and submit the change."
default_sling_formula = "mol-jj-worker-work"
idle_timeout = "2h"
min_active_sessions = 1
max_active_sessions = 4
