scope = "rig"
provider = "kimi-for-coding"
wake_mode = "fresh"
work_dir = "{{.RigRoot}}/.t3-dev/worktrees/gascity/{{.CityName}}/{{.Rig}}/jj/agents/sentinel"
nudge = "Run gc prime, inspect JJ workspace health and submitted-change readiness, then repair or route work."
idle_timeout = "1h"
max_active_sessions = 1
