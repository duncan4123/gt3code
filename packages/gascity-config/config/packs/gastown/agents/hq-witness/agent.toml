scope = "city"
wake_mode = "fresh"
work_dir = ".gc/agents/hq-witness"
nudge = "Run 'gc prime' to check workspace worker status and begin HQ witness patrol."
idle_timeout = "1h"
max_active_sessions = 1

