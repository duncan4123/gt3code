scope = "city"
wake_mode = "resume"
work_dir = ".gc/worktrees/hq-polecats/{{.AgentBase}}"
pre_start = [
  "{{.ConfigDir}}/assets/scripts/worktree-setup.sh \"$(cd '{{.CityRoot}}/../../../..' && pwd)\" {{.WorkDir}} {{.AgentBase}} --sync",
]
idle_timeout = "2h"
max_active_sessions = 5

