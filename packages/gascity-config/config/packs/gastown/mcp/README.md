Shared MCP catalogs are intentionally empty for the bundled T3Code t3bridge
city. Attach MCP servers under agent-local mcp/ directories so worktree-backed
agents are not blocked by provider-native projection checks.
